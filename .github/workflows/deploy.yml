name: Deploy Monitor Toner

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy no servidor
        uses: appleboy/ssh-action@v1.2.0

        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_KEY }}

          script: |
            cd /home/infra/infra/monitor-toner

            git fetch origin

            git reset --hard origin/main

            docker compose up -d --build

            docker image prune -fname: Deploy Monitor Toner

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy no servidor
        uses: appleboy/ssh-action@v1.2.0

        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_KEY }}

          script: |
            cd /home/infra/infra/monitor-toner

            git fetch origin

            git reset --hard origin/main

            docker compose up -d --build

            docker image prune -f
